# legacy-peer-deps=true
#
# Required because `recharts@2.x` declares a peer dependency on
# react@"^16 || ^17 || ^18" without explicitly listing 18.3.x.
# Removing this flag causes `npm install` to fail with a peer conflict.
# Safe to keep — it does not affect runtime behaviour.
legacy-peer-deps=true
